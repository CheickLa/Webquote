{# templates/components/EmailButtonLink.html.twig #}
<twig:ButtonLink href="{{ href }}" onclick="return confirm('Vous êtes sur le point de renvoyer un mail au client');" id="{{ id }}" class="{{ class }}">
  <i class="ti ti-mail"></i>
</twig:ButtonLink>
